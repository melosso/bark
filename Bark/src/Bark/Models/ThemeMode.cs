namespace Bark.Models;

/// <summary>Forced color scheme for a theme name; <see cref="Auto"/> keeps the OS/localStorage toggle behavior.</summary>
public enum ThemeMode
{
    Auto = 0,
    Light = 1,
    Dark = 2,
}
