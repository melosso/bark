---
title: Installation
---

# Installation

You can run Bark using the .NET CLI:

```bash
dotnet run --project src/Bark
```

Then open `http://localhost:5000` in your browser.

## Configuration

Create an `appsettings.json` to override options.
